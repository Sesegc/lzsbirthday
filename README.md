# 生日惊喜单页

## 本地运行

这个页面不依赖任何安装环境，直接用浏览器打开 `index.html` 就能运行。

也可以用任意本地静态服务打开，例如 VS Code 的 Live Server。

## 页面流程

1. 输入名字和出生年月日
2. 完成 10 道输入题
3. 获得 3 次九宫格抽奖机会

## 自定义内容

- 题目：修改 `script.js` 里的 `quizQuestions`
- 抽奖项：修改 `script.js` 里的 `prizeItems`
- 样式：修改 `style.css`
